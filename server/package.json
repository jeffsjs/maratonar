{
  "name": "json-server-test",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "debug": "nodemon --inspect server.js"
  },
  "devDependencies": {
    "json-server": "^0.15.0"
  }
}
